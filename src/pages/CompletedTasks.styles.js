import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles(theme => ({
    CompletedDateFilter: {
        padding: "20px",
        margin: "auto", 
        textAlign: "center",
        display: "flex",
        width: "350px"
    }
}))