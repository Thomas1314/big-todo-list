import React from 'react';

import List from '../components/List';
import { useSelector } from 'react-redux';

/* const ListWrapp = ({ isListDone }) => {
    const { categories, tasks } = ListHook();
} */