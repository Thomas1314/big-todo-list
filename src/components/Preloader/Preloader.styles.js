import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles(() => ({
  preloader: {
    backgroundColor: 'white',
    width: '200px',
    display: 'block',
    marginLeft: 'auto',
    marginRight: 'auto',
  },
}));
